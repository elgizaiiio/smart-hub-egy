

## تحسين قائمة (+) في الشات لتكون متجاوبة واحترافية

### المشكلة
القائمة حالياً بعرض ثابت `w-64` (256px) وتظهر كبيرة على بعض الشاشات، وغير متجاوبة مع أحجام الأجهزة المختلفة.

### الحل
تعديل القائمة في `src/pages/ChatPage.tsx` (سطر 336-435):

1. **عرض متجاوب**: تغيير من `w-64` إلى `w-[min(280px,calc(100vw-2rem))]` لتناسب جميع الشاشات
2. **ارتفاع محدود مع تمرير**: إضافة `max-h-[min(70vh,500px)] overflow-y-auto` لمنع تجاوز الشاشة
3. **تصغير العناصر**: تقليل padding وأحجام الأيقونات قليلاً لتكون أكثر إحكاماً
4. **تحسين التباعد**: ضبط المسافات بين الأقسام (Attach, Tools, Model, Modes, Integrations)
5. **أيقونات أصغر**: تقليل حجم الدوائر من `w-7 h-7` إلى `w-6 h-6` والأيقونات من `w-3.5` إلى `w-3`

### الملف المتأثر
- `src/pages/ChatPage.tsx` — تعديل CSS classes للقائمة المنبثقة

